# kintai-notifier

```
$ bundle install -j4 --path vendor/bundle
$ # bundle exec ruby notifier.rb
$ make setup
$ launchctl list | grep 'kintaiNotifier'
$ # make teardown
$ # launchctl list | grep 'kintaiNotifier'
```
